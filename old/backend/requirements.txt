# Sia Backend Dependencies

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Validation (included with FastAPI, but explicit)
pydantic>=2.5.0

# HTTP client (for hooks to call the server)
requests>=2.31.0

# Optional: async HTTP client
httpx>=0.26.0
